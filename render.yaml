services:
  - type: web
    name: tag-binance-relay
    runtime: docker
    region: frankfurt
    # Render currently defaults an omitted plan to Starter.
    # Add `plan: free` here only for testing; sleeping stops live liquidation tracking.
    healthCheckPath: /health
    autoDeployTrigger: commit
