name: Test TAG relay

on:
  push:
    branches:
      - main
      - v2.8.0-rc3-cost-safe
      - v2.8.1-rc4-manual-live
      - v2.8.2-rc5-fast-manual
      - v2.8.3-rc6-bounded-neon
      - v2.8.4-rc6-1-startup-grade-audit
      - v2.8.5-rc6-2-chad-reactivation-gate
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compile Python
        run: python -m compileall -q app

      - name: Run unit tests
        run: python -m unittest discover -s tests -v
