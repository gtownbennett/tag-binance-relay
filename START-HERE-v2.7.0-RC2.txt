TAG RELAY 2.7.0 RC2 — START HERE

SAFE DEPLOYMENT ORDER
1. Create a relay backup branch from the currently deployed main branch.
2. Create a separate v2.7.0-rc2-paper-social branch.
3. Upload this clean source to that branch; do not replace live main first.
4. Configure PostgreSQL and preserve the existing RELAY_TOKEN.
5. Add a separate ADMIN_KEY.
6. Add CMC_PRO_API_KEY and the verified CMC_TAG_ID only when official CMC automation is desired.
7. Run the Python test suite and inspect the OpenAPI routes.
8. Deploy the RC2 branch to a preview or controlled Render deployment.
9. Verify /health, /v1/tag/terminal, /v1/tag/predictions/unified, /v1/tag/paper and /v1/tag/social.
10. Install the matching Android RC2 only after relay validation.

IMPORTANT
- This is paper simulation only. It cannot execute a real trade.
- CMC automatic social-post collection is not available until eligible official API credentials are configured.
- Existing PostgreSQL history is preserved; the new tables are additive.
- Do not delete the working relay backup branch.
